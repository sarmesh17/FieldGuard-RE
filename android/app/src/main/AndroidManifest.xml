<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--
      ACCESS_BACKGROUND_LOCATION intentionally NOT requested.

      Play Store treats it as a most-scrutinised permission — requires a
      Permission Declaration form, screen-recording video, and a 3–7 day
      review. We don't need it: location streaming runs inside a
      FOREGROUND_SERVICE_LOCATION foreground service (started while a task is
      IN_PROGRESS), which Android treats as foreground for permission
      purposes. The service's persistent notification keeps tracking alive
      while the user is in WhatsApp/other apps; only a manual force-stop
      ends it.

      If you ever genuinely need post-force-stop wake-up (OS GeofencingClient
      based on a registered geofence), re-add this permission and prepare the
      Play declaration. Until then, leaving it out keeps store review easy.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <application
        android:label="FieldGuard"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Mark the flutter_background_service service as a location-type
             foreground service (required on Android 14+). Overrides the
             plugin-declared service via manifest merge. -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="location"
            android:stopWithTask="false"
            tools:replace="android:stopWithTask" />

        <!-- Remove the plugin's BOOT_COMPLETED receiver. On Android 14+ a
             location foreground service CANNOT be started from a boot receiver
             (ForegroundServiceStartNotAllowedException → crash). Our tracking
             is user/task-driven and requires the app to be in the foreground to
             start, so reboot auto-restart isn't needed (and was crashing). This
             does NOT affect swipe-kill survival (handled by stopWithTask=false
             + START_STICKY), only device-reboot restart. -->
        <receiver
            android:name="id.flutter.flutter_background_service.BootReceiver"
            tools:node="remove" />

        <!-- FCM: small icon + accent colour for notifications the OS renders
             itself (background/terminated). Foreground pushes are shown by
             flutter_local_notifications, which use the same drawable. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="push_alerts" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
